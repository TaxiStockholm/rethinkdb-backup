rethinkdb_backup:
  build: .
  environment:
    - CRON_TIME=* * * * *
  links:
    - rethinkdb
  # volumes_from:
    # - rethinkdb
  volumes:
    - /backup:/backup
rethinkdb:
  image: 'rethinkdb:2.2.0'
  command: 'rethinkdb --bind all'
  ports:
    - '28015:28015'
  restart: always
